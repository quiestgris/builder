{# templates/product/new.html.twig #}

{% block title %}Créer un produit{% endblock %}

{% block body %}
    <h1>Créer un service</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('show_service') }}">← Retour à la liste</a>
{% endblock %}